[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
scripts = { traceboard = "traceboard.cli.main:main" }
name = "traceboard"
version = "0.1.0"
description = "Traceboard: Design-system governance control room (core domain)"
readme = "README.md"
license = { text = "Proprietary" }
requires-python = ">=3.11"
authors = [
  { name = "Perplexity Design Systems", email = "design-systems@perplexity.ai" }
]
dependencies = [
  "pydantic>=2.13.5",
  "streamlit>=1.52.0,<2.0.0",
]

[project.optional-dependencies]
dev = [
  "pre-commit>=4.6.2",
  "ruff>=0.9.0",
  "pyright>=1.1.414",
  "pytest>=9.1.1",
  "pytest-asyncio>=0.24.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/traceboard"]

# -------------------------
# Ruff configuration
# -------------------------
[tool.ruff]
src = ["src"]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
extend-select = [
  "E",      # pycodestyle errors
  "W",      # pycodestyle warnings
  "F",      # Pyflakes
  "I",      # isort
  "B",      # flake8-bugbear
  "C4",     # flake8-comprehensions
  "UP",     # pyupgrade
  "TCH",    # flake8-type-checking
  "RUF",    # ruff-specific rules
  "SIM",    # flake8-simplify
  "PTH",    # flake8-use-pathlib
]
ignore = [
  "E501",   # line-length (we use formatter)
]

[tool.ruff.lint.isort]
known-first-party = ["traceboard"]
required-imports = ["from __future__ import annotations"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"

# -------------------------
# Pyright configuration
# -------------------------
[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "strict"
include = ["src/traceboard"]
exclude = [
  "**/__pycache__",
  "**/.venv",
  "**/node_modules",
]
reportMissingTypeStubs = false
reportImplicitStringConcatenation = false

# -------------------------
# Pytest configuration
# -------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "streamlit (>=1.64.0,<2.0.0)",
    "pytest-cov (>=7.1.0,<8.0.0)"
]

[tool.coverage.run]
source = ["src/traceboard"]
branch = true
omit = [
  "*/tests/*",
  "*/__pycache__/*",
]

[tool.coverage.report]
fail_under = 60
show_missing = true
skip_covered = true
exclude_lines = [
  "pragma: no cover",
  "if TYPE_CHECKING:",
  "def __repr__",
  "raise NotImplementedError",
  "if __name__ == .__main__.:",
]

[tool.coverage.run]
source = ["src/traceboard"]
branch = true
omit = [
  "*/tests/*",
  "*/__pycache__/*",
]

[tool.coverage.report]
fail_under = 60
show_missing = true
skip_covered = true
exclude_lines = [
  "pragma: no cover",
  "if TYPE_CHECKING:",
  "def __repr__",
  "raise NotImplementedError",
  "if __name__ == .__main__.:",
]
